import UIKit

struct OCTextInputDefaults {
    
    let padding: CGFloat = -16.0
    
    struct ClearButton {
        let padding: CGFloat = -8.0

    }
    
    struct Placeholder {
        let text        = "Enter text"
        let animate     = true
        let duration    = 0.15
    }
}
